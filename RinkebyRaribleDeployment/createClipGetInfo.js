require('dotenv').config();
const axios = require('axios');

async function refreshAccessToken(){
    const url = 'https://id.twitch.tv/oauth2/token';
    let refresh = process.env.
    p={grant_type: refresh_token, refresh_token:}
}